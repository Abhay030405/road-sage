"""UFLDv2 (Ultra-Fast Lane Detection v2) model wrapper.

Phase 5 — inference not yet implemented.

The model accepts a normalised (800 × 288) RGB tensor and returns per-row
lane coordinate predictions across ``num_lanes`` lanes and ``num_row_anchors``
row positions.  Full implementation deferred to Phase 5 when model weights
are available.
"""

# Phase 1 placeholder — nothing to import until model weights are downloaded.
